// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"

	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"rei.io/rei/ent/nft"
)

// NFTCreate is the builder for creating a NFT entity.
type NFTCreate struct {
	config
	mutation *NFTMutation
	hooks    []Hook
}

// SetObjectID sets the "ObjectID" field.
func (nc *NFTCreate) SetObjectID(s string) *NFTCreate {
	nc.mutation.SetObjectID(s)
	return nc
}

// SetType sets the "Type" field.
func (nc *NFTCreate) SetType(s string) *NFTCreate {
	nc.mutation.SetType(s)
	return nc
}

// SetMetadata sets the "Metadata" field.
func (nc *NFTCreate) SetMetadata(m map[string]interface{}) *NFTCreate {
	nc.mutation.SetMetadata(m)
	return nc
}

// SetSequenceID sets the "SequenceID" field.
func (nc *NFTCreate) SetSequenceID(u uint64) *NFTCreate {
	nc.mutation.SetSequenceID(u)
	return nc
}

// Mutation returns the NFTMutation object of the builder.
func (nc *NFTCreate) Mutation() *NFTMutation {
	return nc.mutation
}

// Save creates the NFT in the database.
func (nc *NFTCreate) Save(ctx context.Context) (*NFT, error) {
	var (
		err  error
		node *NFT
	)
	if len(nc.hooks) == 0 {
		if err = nc.check(); err != nil {
			return nil, err
		}
		node, err = nc.sqlSave(ctx)
	} else {
		var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
			mutation, ok := m.(*NFTMutation)
			if !ok {
				return nil, fmt.Errorf("unexpected mutation type %T", m)
			}
			if err = nc.check(); err != nil {
				return nil, err
			}
			nc.mutation = mutation
			if node, err = nc.sqlSave(ctx); err != nil {
				return nil, err
			}
			mutation.id = &node.ID
			mutation.done = true
			return node, err
		})
		for i := len(nc.hooks) - 1; i >= 0; i-- {
			if nc.hooks[i] == nil {
				return nil, fmt.Errorf("ent: uninitialized hook (forgotten import ent/runtime?)")
			}
			mut = nc.hooks[i](mut)
		}
		v, err := mut.Mutate(ctx, nc.mutation)
		if err != nil {
			return nil, err
		}
		nv, ok := v.(*NFT)
		if !ok {
			return nil, fmt.Errorf("unexpected node type %T returned from NFTMutation", v)
		}
		node = nv
	}
	return node, err
}

// SaveX calls Save and panics if Save returns an error.
func (nc *NFTCreate) SaveX(ctx context.Context) *NFT {
	v, err := nc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (nc *NFTCreate) Exec(ctx context.Context) error {
	_, err := nc.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (nc *NFTCreate) ExecX(ctx context.Context) {
	if err := nc.Exec(ctx); err != nil {
		panic(err)
	}
}

// check runs all checks and user-defined validators on the builder.
func (nc *NFTCreate) check() error {
	if _, ok := nc.mutation.ObjectID(); !ok {
		return &ValidationError{Name: "ObjectID", err: errors.New(`ent: missing required field "NFT.ObjectID"`)}
	}
	if _, ok := nc.mutation.GetType(); !ok {
		return &ValidationError{Name: "Type", err: errors.New(`ent: missing required field "NFT.Type"`)}
	}
	if _, ok := nc.mutation.Metadata(); !ok {
		return &ValidationError{Name: "Metadata", err: errors.New(`ent: missing required field "NFT.Metadata"`)}
	}
	if _, ok := nc.mutation.SequenceID(); !ok {
		return &ValidationError{Name: "SequenceID", err: errors.New(`ent: missing required field "NFT.SequenceID"`)}
	}
	return nil
}

func (nc *NFTCreate) sqlSave(ctx context.Context) (*NFT, error) {
	_node, _spec := nc.createSpec()
	if err := sqlgraph.CreateNode(ctx, nc.driver, _spec); err != nil {
		if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{msg: err.Error(), wrap: err}
		}
		return nil, err
	}
	id := _spec.ID.Value.(int64)
	_node.ID = int(id)
	return _node, nil
}

func (nc *NFTCreate) createSpec() (*NFT, *sqlgraph.CreateSpec) {
	var (
		_node = &NFT{config: nc.config}
		_spec = &sqlgraph.CreateSpec{
			Table: nft.Table,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: nft.FieldID,
			},
		}
	)
	if value, ok := nc.mutation.ObjectID(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeString,
			Value:  value,
			Column: nft.FieldObjectID,
		})
		_node.ObjectID = value
	}
	if value, ok := nc.mutation.GetType(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeString,
			Value:  value,
			Column: nft.FieldType,
		})
		_node.Type = value
	}
	if value, ok := nc.mutation.Metadata(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeJSON,
			Value:  value,
			Column: nft.FieldMetadata,
		})
		_node.Metadata = value
	}
	if value, ok := nc.mutation.SequenceID(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeUint64,
			Value:  value,
			Column: nft.FieldSequenceID,
		})
		_node.SequenceID = value
	}
	return _node, _spec
}

// NFTCreateBulk is the builder for creating many NFT entities in bulk.
type NFTCreateBulk struct {
	config
	builders []*NFTCreate
}

// Save creates the NFT entities in the database.
func (ncb *NFTCreateBulk) Save(ctx context.Context) ([]*NFT, error) {
	specs := make([]*sqlgraph.CreateSpec, len(ncb.builders))
	nodes := make([]*NFT, len(ncb.builders))
	mutators := make([]Mutator, len(ncb.builders))
	for i := range ncb.builders {
		func(i int, root context.Context) {
			builder := ncb.builders[i]
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*NFTMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				nodes[i], specs[i] = builder.createSpec()
				var err error
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, ncb.builders[i+1].mutation)
				} else {
					spec := &sqlgraph.BatchCreateSpec{Nodes: specs}
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, ncb.driver, spec); err != nil {
						if sqlgraph.IsConstraintError(err) {
							err = &ConstraintError{msg: err.Error(), wrap: err}
						}
					}
				}
				if err != nil {
					return nil, err
				}
				mutation.id = &nodes[i].ID
				if specs[i].ID.Value != nil {
					id := specs[i].ID.Value.(int64)
					nodes[i].ID = int(id)
				}
				mutation.done = true
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, ncb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (ncb *NFTCreateBulk) SaveX(ctx context.Context) []*NFT {
	v, err := ncb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (ncb *NFTCreateBulk) Exec(ctx context.Context) error {
	_, err := ncb.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (ncb *NFTCreateBulk) ExecX(ctx context.Context) {
	if err := ncb.Exec(ctx); err != nil {
		panic(err)
	}
}
