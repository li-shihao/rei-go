// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"

	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"rei.io/rei/ent/packages"
)

// PackagesCreate is the builder for creating a Packages entity.
type PackagesCreate struct {
	config
	mutation *PackagesMutation
	hooks    []Hook
}

// SetTransactionID sets the "TransactionID" field.
func (pc *PackagesCreate) SetTransactionID(s string) *PackagesCreate {
	pc.mutation.SetTransactionID(s)
	return pc
}

// SetObjectID sets the "ObjectID" field.
func (pc *PackagesCreate) SetObjectID(s string) *PackagesCreate {
	pc.mutation.SetObjectID(s)
	return pc
}

// SetBytecode sets the "Bytecode" field.
func (pc *PackagesCreate) SetBytecode(m map[string]interface{}) *PackagesCreate {
	pc.mutation.SetBytecode(m)
	return pc
}

// Mutation returns the PackagesMutation object of the builder.
func (pc *PackagesCreate) Mutation() *PackagesMutation {
	return pc.mutation
}

// Save creates the Packages in the database.
func (pc *PackagesCreate) Save(ctx context.Context) (*Packages, error) {
	var (
		err  error
		node *Packages
	)
	if len(pc.hooks) == 0 {
		if err = pc.check(); err != nil {
			return nil, err
		}
		node, err = pc.sqlSave(ctx)
	} else {
		var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
			mutation, ok := m.(*PackagesMutation)
			if !ok {
				return nil, fmt.Errorf("unexpected mutation type %T", m)
			}
			if err = pc.check(); err != nil {
				return nil, err
			}
			pc.mutation = mutation
			if node, err = pc.sqlSave(ctx); err != nil {
				return nil, err
			}
			mutation.id = &node.ID
			mutation.done = true
			return node, err
		})
		for i := len(pc.hooks) - 1; i >= 0; i-- {
			if pc.hooks[i] == nil {
				return nil, fmt.Errorf("ent: uninitialized hook (forgotten import ent/runtime?)")
			}
			mut = pc.hooks[i](mut)
		}
		v, err := mut.Mutate(ctx, pc.mutation)
		if err != nil {
			return nil, err
		}
		nv, ok := v.(*Packages)
		if !ok {
			return nil, fmt.Errorf("unexpected node type %T returned from PackagesMutation", v)
		}
		node = nv
	}
	return node, err
}

// SaveX calls Save and panics if Save returns an error.
func (pc *PackagesCreate) SaveX(ctx context.Context) *Packages {
	v, err := pc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (pc *PackagesCreate) Exec(ctx context.Context) error {
	_, err := pc.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (pc *PackagesCreate) ExecX(ctx context.Context) {
	if err := pc.Exec(ctx); err != nil {
		panic(err)
	}
}

// check runs all checks and user-defined validators on the builder.
func (pc *PackagesCreate) check() error {
	if _, ok := pc.mutation.TransactionID(); !ok {
		return &ValidationError{Name: "TransactionID", err: errors.New(`ent: missing required field "Packages.TransactionID"`)}
	}
	if _, ok := pc.mutation.ObjectID(); !ok {
		return &ValidationError{Name: "ObjectID", err: errors.New(`ent: missing required field "Packages.ObjectID"`)}
	}
	if _, ok := pc.mutation.Bytecode(); !ok {
		return &ValidationError{Name: "Bytecode", err: errors.New(`ent: missing required field "Packages.Bytecode"`)}
	}
	return nil
}

func (pc *PackagesCreate) sqlSave(ctx context.Context) (*Packages, error) {
	_node, _spec := pc.createSpec()
	if err := sqlgraph.CreateNode(ctx, pc.driver, _spec); err != nil {
		if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{msg: err.Error(), wrap: err}
		}
		return nil, err
	}
	id := _spec.ID.Value.(int64)
	_node.ID = int(id)
	return _node, nil
}

func (pc *PackagesCreate) createSpec() (*Packages, *sqlgraph.CreateSpec) {
	var (
		_node = &Packages{config: pc.config}
		_spec = &sqlgraph.CreateSpec{
			Table: packages.Table,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: packages.FieldID,
			},
		}
	)
	if value, ok := pc.mutation.TransactionID(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeString,
			Value:  value,
			Column: packages.FieldTransactionID,
		})
		_node.TransactionID = value
	}
	if value, ok := pc.mutation.ObjectID(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeString,
			Value:  value,
			Column: packages.FieldObjectID,
		})
		_node.ObjectID = value
	}
	if value, ok := pc.mutation.Bytecode(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeJSON,
			Value:  value,
			Column: packages.FieldBytecode,
		})
		_node.Bytecode = value
	}
	return _node, _spec
}

// PackagesCreateBulk is the builder for creating many Packages entities in bulk.
type PackagesCreateBulk struct {
	config
	builders []*PackagesCreate
}

// Save creates the Packages entities in the database.
func (pcb *PackagesCreateBulk) Save(ctx context.Context) ([]*Packages, error) {
	specs := make([]*sqlgraph.CreateSpec, len(pcb.builders))
	nodes := make([]*Packages, len(pcb.builders))
	mutators := make([]Mutator, len(pcb.builders))
	for i := range pcb.builders {
		func(i int, root context.Context) {
			builder := pcb.builders[i]
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*PackagesMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				nodes[i], specs[i] = builder.createSpec()
				var err error
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, pcb.builders[i+1].mutation)
				} else {
					spec := &sqlgraph.BatchCreateSpec{Nodes: specs}
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, pcb.driver, spec); err != nil {
						if sqlgraph.IsConstraintError(err) {
							err = &ConstraintError{msg: err.Error(), wrap: err}
						}
					}
				}
				if err != nil {
					return nil, err
				}
				mutation.id = &nodes[i].ID
				if specs[i].ID.Value != nil {
					id := specs[i].ID.Value.(int64)
					nodes[i].ID = int(id)
				}
				mutation.done = true
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, pcb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (pcb *PackagesCreateBulk) SaveX(ctx context.Context) []*Packages {
	v, err := pcb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (pcb *PackagesCreateBulk) Exec(ctx context.Context) error {
	_, err := pcb.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (pcb *PackagesCreateBulk) ExecX(ctx context.Context) {
	if err := pcb.Exec(ctx); err != nil {
		panic(err)
	}
}
