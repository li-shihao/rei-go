// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"rei.io/rei/ent/packages"
	"rei.io/rei/ent/predicate"
)

// PackagesUpdate is the builder for updating Packages entities.
type PackagesUpdate struct {
	config
	hooks    []Hook
	mutation *PackagesMutation
}

// Where appends a list predicates to the PackagesUpdate builder.
func (pu *PackagesUpdate) Where(ps ...predicate.Packages) *PackagesUpdate {
	pu.mutation.Where(ps...)
	return pu
}

// SetTransactionID sets the "TransactionID" field.
func (pu *PackagesUpdate) SetTransactionID(s string) *PackagesUpdate {
	pu.mutation.SetTransactionID(s)
	return pu
}

// SetObjectID sets the "ObjectID" field.
func (pu *PackagesUpdate) SetObjectID(s string) *PackagesUpdate {
	pu.mutation.SetObjectID(s)
	return pu
}

// SetBytecode sets the "Bytecode" field.
func (pu *PackagesUpdate) SetBytecode(m map[string]interface{}) *PackagesUpdate {
	pu.mutation.SetBytecode(m)
	return pu
}

// Mutation returns the PackagesMutation object of the builder.
func (pu *PackagesUpdate) Mutation() *PackagesMutation {
	return pu.mutation
}

// Save executes the query and returns the number of nodes affected by the update operation.
func (pu *PackagesUpdate) Save(ctx context.Context) (int, error) {
	var (
		err      error
		affected int
	)
	if len(pu.hooks) == 0 {
		affected, err = pu.sqlSave(ctx)
	} else {
		var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
			mutation, ok := m.(*PackagesMutation)
			if !ok {
				return nil, fmt.Errorf("unexpected mutation type %T", m)
			}
			pu.mutation = mutation
			affected, err = pu.sqlSave(ctx)
			mutation.done = true
			return affected, err
		})
		for i := len(pu.hooks) - 1; i >= 0; i-- {
			if pu.hooks[i] == nil {
				return 0, fmt.Errorf("ent: uninitialized hook (forgotten import ent/runtime?)")
			}
			mut = pu.hooks[i](mut)
		}
		if _, err := mut.Mutate(ctx, pu.mutation); err != nil {
			return 0, err
		}
	}
	return affected, err
}

// SaveX is like Save, but panics if an error occurs.
func (pu *PackagesUpdate) SaveX(ctx context.Context) int {
	affected, err := pu.Save(ctx)
	if err != nil {
		panic(err)
	}
	return affected
}

// Exec executes the query.
func (pu *PackagesUpdate) Exec(ctx context.Context) error {
	_, err := pu.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (pu *PackagesUpdate) ExecX(ctx context.Context) {
	if err := pu.Exec(ctx); err != nil {
		panic(err)
	}
}

func (pu *PackagesUpdate) sqlSave(ctx context.Context) (n int, err error) {
	_spec := &sqlgraph.UpdateSpec{
		Node: &sqlgraph.NodeSpec{
			Table:   packages.Table,
			Columns: packages.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: packages.FieldID,
			},
		},
	}
	if ps := pu.mutation.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if value, ok := pu.mutation.TransactionID(); ok {
		_spec.Fields.Set = append(_spec.Fields.Set, &sqlgraph.FieldSpec{
			Type:   field.TypeString,
			Value:  value,
			Column: packages.FieldTransactionID,
		})
	}
	if value, ok := pu.mutation.ObjectID(); ok {
		_spec.Fields.Set = append(_spec.Fields.Set, &sqlgraph.FieldSpec{
			Type:   field.TypeString,
			Value:  value,
			Column: packages.FieldObjectID,
		})
	}
	if value, ok := pu.mutation.Bytecode(); ok {
		_spec.Fields.Set = append(_spec.Fields.Set, &sqlgraph.FieldSpec{
			Type:   field.TypeJSON,
			Value:  value,
			Column: packages.FieldBytecode,
		})
	}
	if n, err = sqlgraph.UpdateNodes(ctx, pu.driver, _spec); err != nil {
		if _, ok := err.(*sqlgraph.NotFoundError); ok {
			err = &NotFoundError{packages.Label}
		} else if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{msg: err.Error(), wrap: err}
		}
		return 0, err
	}
	return n, nil
}

// PackagesUpdateOne is the builder for updating a single Packages entity.
type PackagesUpdateOne struct {
	config
	fields   []string
	hooks    []Hook
	mutation *PackagesMutation
}

// SetTransactionID sets the "TransactionID" field.
func (puo *PackagesUpdateOne) SetTransactionID(s string) *PackagesUpdateOne {
	puo.mutation.SetTransactionID(s)
	return puo
}

// SetObjectID sets the "ObjectID" field.
func (puo *PackagesUpdateOne) SetObjectID(s string) *PackagesUpdateOne {
	puo.mutation.SetObjectID(s)
	return puo
}

// SetBytecode sets the "Bytecode" field.
func (puo *PackagesUpdateOne) SetBytecode(m map[string]interface{}) *PackagesUpdateOne {
	puo.mutation.SetBytecode(m)
	return puo
}

// Mutation returns the PackagesMutation object of the builder.
func (puo *PackagesUpdateOne) Mutation() *PackagesMutation {
	return puo.mutation
}

// Select allows selecting one or more fields (columns) of the returned entity.
// The default is selecting all fields defined in the entity schema.
func (puo *PackagesUpdateOne) Select(field string, fields ...string) *PackagesUpdateOne {
	puo.fields = append([]string{field}, fields...)
	return puo
}

// Save executes the query and returns the updated Packages entity.
func (puo *PackagesUpdateOne) Save(ctx context.Context) (*Packages, error) {
	var (
		err  error
		node *Packages
	)
	if len(puo.hooks) == 0 {
		node, err = puo.sqlSave(ctx)
	} else {
		var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
			mutation, ok := m.(*PackagesMutation)
			if !ok {
				return nil, fmt.Errorf("unexpected mutation type %T", m)
			}
			puo.mutation = mutation
			node, err = puo.sqlSave(ctx)
			mutation.done = true
			return node, err
		})
		for i := len(puo.hooks) - 1; i >= 0; i-- {
			if puo.hooks[i] == nil {
				return nil, fmt.Errorf("ent: uninitialized hook (forgotten import ent/runtime?)")
			}
			mut = puo.hooks[i](mut)
		}
		v, err := mut.Mutate(ctx, puo.mutation)
		if err != nil {
			return nil, err
		}
		nv, ok := v.(*Packages)
		if !ok {
			return nil, fmt.Errorf("unexpected node type %T returned from PackagesMutation", v)
		}
		node = nv
	}
	return node, err
}

// SaveX is like Save, but panics if an error occurs.
func (puo *PackagesUpdateOne) SaveX(ctx context.Context) *Packages {
	node, err := puo.Save(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// Exec executes the query on the entity.
func (puo *PackagesUpdateOne) Exec(ctx context.Context) error {
	_, err := puo.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (puo *PackagesUpdateOne) ExecX(ctx context.Context) {
	if err := puo.Exec(ctx); err != nil {
		panic(err)
	}
}

func (puo *PackagesUpdateOne) sqlSave(ctx context.Context) (_node *Packages, err error) {
	_spec := &sqlgraph.UpdateSpec{
		Node: &sqlgraph.NodeSpec{
			Table:   packages.Table,
			Columns: packages.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: packages.FieldID,
			},
		},
	}
	id, ok := puo.mutation.ID()
	if !ok {
		return nil, &ValidationError{Name: "id", err: errors.New(`ent: missing "Packages.id" for update`)}
	}
	_spec.Node.ID.Value = id
	if fields := puo.fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, packages.FieldID)
		for _, f := range fields {
			if !packages.ValidColumn(f) {
				return nil, &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
			}
			if f != packages.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, f)
			}
		}
	}
	if ps := puo.mutation.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if value, ok := puo.mutation.TransactionID(); ok {
		_spec.Fields.Set = append(_spec.Fields.Set, &sqlgraph.FieldSpec{
			Type:   field.TypeString,
			Value:  value,
			Column: packages.FieldTransactionID,
		})
	}
	if value, ok := puo.mutation.ObjectID(); ok {
		_spec.Fields.Set = append(_spec.Fields.Set, &sqlgraph.FieldSpec{
			Type:   field.TypeString,
			Value:  value,
			Column: packages.FieldObjectID,
		})
	}
	if value, ok := puo.mutation.Bytecode(); ok {
		_spec.Fields.Set = append(_spec.Fields.Set, &sqlgraph.FieldSpec{
			Type:   field.TypeJSON,
			Value:  value,
			Column: packages.FieldBytecode,
		})
	}
	_node = &Packages{config: puo.config}
	_spec.Assign = _node.assignValues
	_spec.ScanValues = _node.scanValues
	if err = sqlgraph.UpdateNode(ctx, puo.driver, _spec); err != nil {
		if _, ok := err.(*sqlgraph.NotFoundError); ok {
			err = &NotFoundError{packages.Label}
		} else if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{msg: err.Error(), wrap: err}
		}
		return nil, err
	}
	return _node, nil
}
